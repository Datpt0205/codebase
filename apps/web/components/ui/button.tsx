/**
 * The app already has a Button. Generated components import
 * `@/components/ui/button`, so this points that name at the real one rather
 * than letting a second implementation land beside it.
 */
export { Button, buttonVariants, type ButtonProps } from "@dw/ui";
